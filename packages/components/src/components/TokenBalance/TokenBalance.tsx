import React from 'react';
import { Box } from '@chakra-ui/react';

export interface TokenBalanceProps {}

export const TokenBalance = ({}: TokenBalanceProps) => {
  return (
    <Box>
      <p>Hello Balance</p>
    </Box>
  );
};
